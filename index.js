/* Aprendiendo a sumar del 0 al 10*/
let numero = parseInt(prompt("Escribí un número"));

for(let i = 0; i <= 9; i++){
        let resultado = numero + i;
        let mensaje = `${numero} + ${i} = ${resultado}`;
        alert(mensaje);      
}

        
       

